# Sprint 1 交付总结（语义层核心）

> **Sprint 目标**（见 [Sprint计划](../Sprint计划-本体建模平台-v2.0.md)）：S01 / S03 / S04 / S07  
> **核实日期**：2026-06-04 | **分支**：`web-ui` | **实现形态**：MVP 内存仓储 + 部分 JPA（聚合根）

## 1. 新增 API 端点（7 个 — 已落地）

| 端点 | Story | 功能 | 代码位置 |
|------|-------|------|----------|
| `POST /v1/contexts` | S01 | 创建限界上下文，1:1 绑定本体 | `BoundedContextController#create` |
| `GET /v1/contexts/{id}` | S01 | 查询上下文详情 | `BoundedContextController#get` |
| `POST /v1/contexts/{id}/submit-review` | G05 | 提交审核（Draft→InReview） | `BoundedContextController#submitReview` |
| `POST /v1/contexts/{id}/approve` | G05 | 批准发布（InReview→Published） | `BoundedContextController#approve` |
| `POST /v1/contexts/{id}/aggregate-roots` | S03 | 定义聚合根 | `ModelingController#createAggRoot` |
| `POST /v1/contexts/{id}/object-types` | S04 | 定义对象类型 | `ModelingController#createObjType` |
| `POST /v1/contexts/{id}/relationships` | S07 | 定义关系类型（含组合关系校验） | `ModelingController#createRel` |

### 1.1 同期额外端点（未列入上表，已实现）

| 端点 | 说明 |
|------|------|
| `GET /v1/contexts` | 上下文列表 |
| `GET /v1/contexts/{id}/aggregate-roots` | 聚合根列表 |
| `GET /v1/contexts/{id}/object-types` | 对象类型列表 |
| `PUT /v1/contexts/{id}/object-types/{id}/attributes` | S04 属性 JSON 更新 |
| `GET /v1/contexts/{id}/relationships` | 关系列表 |
| `POST /v1/contexts/{id}/reject-review` | G05 打回草稿 |

## 2. 关键实现说明

### US-S07 组合关系校验

```47:52:ontology-application/src/main/java/com/ontology/platform/application/service/ModelingService.java
        if ("COMPOSITION".equals(kind)) {
            ObjectTypeV2 src = getObjectType(sourceId);
            ObjectTypeV2 tgt = getObjectType(targetId);
            if (src.getAggregateRootId() == null || !src.getAggregateRootId().equals(tgt.getAggregateRootId()))
                throw new BusinessException(ErrorCode.VALIDATION_ERROR, "组合关系只能在同一聚合根内的对象之间建立");
```

### 工程化修复

| 项 | 状态 |
|----|------|
| `-parameters`（Spring 路径/参数绑定） | `pom.xml` compiler `arg` |
| H2 测试依赖 | `ontology-api/pom.xml` + `@ActiveProfiles("h2")` |
| `@NoArgsConstructor` JSON 反序列化 | 新建 DTO（如 `AggregateRootCreateRequest`） |

## 3. 主要代码文件（约 14 个核心文件）

| 层 | 文件 |
|----|------|
| API | `BoundedContextController`, `ModelingController`, DTO×4 |
| Application | `BoundedContextService`, `ModelingService` |
| Domain | `BoundedContext`, `AggregateRoot`, `ObjectTypeV2`, `Relationship` |
| Test | `BoundedContextControllerTest`, `ModelingControllerTest` |

## 4. 与 Sprint 1 计划的符合度

| 计划项 | 状态 | 备注 |
|--------|------|------|
| US-S01 | ✅ | 含 1:1 `ontologyId`（`BoundedContextService`） |
| US-S03 | ✅ | 聚合根持久化走 `AggregateRootRepository` |
| US-S04 | ⚠️ 部分 | 对象/属性为**内存 Map**；无拖拽排序 API |
| US-S07 | ✅ | 组合校验已实现 |
| US-S08 数据源 | ⏭️ | 属 **Sprint 2**，本 Sprint 未要求 |
| US-G05 工作流 | ✅ 超前 | 原计划 Sprint 5；当前仅 submit/approve/reject，无批注链 |

## 5. 建议 Sprint 2 入口条件

- [ ] 对象类型/关系迁入 PostgreSQL（对齐 `V2__tdd_v2_schema.sql`）
- [ ] S04：属性结构化 JSONB + 校验（非裸 `String attributes`）
- [ ] G05：补 `review-comments`、`workflow-history`（契约已有路径）
- [ ] 集成测试覆盖 COMPOSITION 校验失败用例
