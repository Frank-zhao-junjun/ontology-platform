# Sprint 2 进行中（地基：数据源 + 治理）

> **计划**：[Sprint计划 § Sprint 2](../Sprint计划-本体建模平台-v2.0.md)  
> **分支**：`web-ui` | **开始**：2026-06-04 | **最后更新**：2026-06-07

## 目标与完成度

| Story | 点 | 状态 | AC 完成度 | 备注 |
|-------|-----|------|----------|------|
| US-S08 数据源与获取方式 | 5 | ✅ 基本完成 | AC1-4 ✅ / AC5 ❌ | 修复了 `contextId` 数据丢失 bug（V2 迁移 + Entity 字段 + Mapper 映射）；AC-5 血缘图 P2 远期 |
| US-G01 角色与对象级权限 | 8 | ✅ 基本完成 | AC1-2 ✅ / AC3-4 ✅ | AC-3 Manifest 编译已添加 permissions 段（2026-06-07） |
| US-G02 字段级权限 | — | ✅ 基本完成 | AC1-2 ⚠️ | 字段权限 CRUD 已实现，方向为"角色→字段"（非 PRD 的"字段→角色"）；AC-2 Manifest 字段过滤已编译到 governance.fieldPermissions 段 |
| US-G04 AI Agent 沙箱 | 5 | ⚠️ 部分完成 | AC1 ✅ / AC2-4 🔄 | AC-1 沙箱配置 CRUD 已实现；AC-2/3（运行时 403/429 限流）→ Sprint 6 A05；AC-4 审计日志 → 已创建 AuditLog 实体+Repository+Service（2026-06-07） |
| 基础：Flyway V1/V2 + JPA 持久化 | 2 | ✅ | — | V2 已添加 `context_id` 列 |

## 2026-06-07 代码完善

### 已修复 P0 Bug
| 修复 | 文件 | 说明 |
|------|------|------|
| `DataAccessMethodEntity` 缺少 `contextId` | `DataAccessMethodEntity.java` + `PersistenceMapper.java` + `V2__...sql` | 数据获取方式保存后上下文信息丢失，修复后 contextId 正确持久化 |

### 已新增功能
| 新增 | 文件 | 说明 |
|------|------|------|
| Manifest 编译添加 `governance` 段 | `ManifestSnapshotService.java` | 编译角色权限矩阵（G01 AC-3）、字段权限（G02 AC-2）、沙箱配置（G04 AC-1）写入 Manifest |
| 审计日志基础设施 | `AuditLog.java` + `AuditLogEntity.java` + `AuditLogRepository.java` + `AuditLogJpaRepository.java` + `JpaAuditLogRepository.java` + `PersistenceMapper.java` | 审计日志领域模型+持久化，为 Sprint 6 运行时审计打好基础 |

## 已落地 API（JPA + H2/PostgreSQL）

| 端点 | Story |
|------|-------|
| `POST /v1/data-sources` | S08 |
| `GET /v1/data-sources` | S08 |
| `POST /v1/contexts/{contextId}/data-access-methods` | S08 |
| `POST /v1/roles` | G01 |
| `GET /v1/roles` | G01 |
| `POST /v1/roles/{roleId}/object-permissions` | G01 AC-2 |
| `POST /v1/sandboxes` | G04 |
| `GET /v1/sandboxes` | G04 |
| `POST /v1/roles/{roleId}/field-permissions` | G02 |
| `GET /v1/roles/{roleId}/field-permissions` | G02 |

## 代码位置

- `DataSourceController` / `DataSourceService`
- `GovernanceController` / `GovernanceService`
- `db/migration/V1__init_schema.sql` + `V2__add_context_id_to_data_access_methods.sql`
- `infrastructure/persistence/entity/*` + `Jpa*Repository`
- `ManifestSnapshotService`（Manifest 编译 + governance 段）
- `domain/entity/AuditLog` + `infrastructure/persistence/entity/AuditLogEntity` + `JpaAuditLogRepository`
- `ApiKeyAuthFilter`（`ontology.api.auth.enabled=true` 时启用）

## 待办（后续 Sprint）

- [ ] G03 条件权限 API（P2, Sprint 9）
- [ ] AC-5 数据血缘图（P2, Sprint 9）
- [ ] G04 AC-2/3 沙箱运行时权限校验+限流（Sprint 6 A05 三通道防御）
- [ ] G04 AC-4 审计日志写入（Sprint 6 A05 运行时集成）
- [ ] G02 字段权限设计方向调整（字段→角色，P1 Sprint 8）
- [ ] prod 环境 Flyway 全量迁移验收

## 测试

- `GovernanceControllerTest`（h2 profile，S08+G01+G04 串联）
