package com.ontology.platform.common.enums;

/**
 * 图遍历方向枚举
 * Graph Traversal Direction
 */
public enum TraversalDirection {
    OUTGOING,   // 出边（正向）
    INCOMING,   // 入边（反向）
    BOTH        // 双向
}
