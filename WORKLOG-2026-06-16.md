# 工作日志 — ontology-platform 环境配置与文档更新

**日期**: 2026-06-16  
**分支**: `appmod/java-upgrade-20260608142931`  
**提交者**: AI Assistant  
**相关提交**: `.coze` 配置、README 更新

---

## 一、总体进度

| 任务 | 状态 | 新增 | 修改 |
|------|:----:|:----:|:----:|
| 安装 Java 运行时 (JDK 21 + Maven) | ✅ | 0 | 0 |
| 创建 `.coze` 配置文件 | ✅ | 1 | 0 |
| 创建 `application-dev.yml` 开发配置 | ✅ | 1 | 0 |
| 更新 README.md | ✅ | 0 | 1 |
| **合计** | | **2** | **1** |

---

## 二、详细变更

### 1. 安装 Java 运行时

**执行命令**:
```bash
apt-get update
apt-get install -y openjdk-21-jdk maven
```

**安装版本**:
| 组件 | 版本 |
|------|------|
| OpenJDK | 21.0.11 |
| Maven | 3.8.7 |

**验证**:
```bash
$ java -version
openjdk version "21.0.11" 2026-04-21

$ mvn -version
Apache Maven 3.8.7
```

---

### 2. 创建 `.coze` 配置文件

**文件**: `.coze` (项目根目录)

**配置内容**:
```toml
[project]
requires = ["java-21", "maven-3.8"]
entrypoint = "ontology-api/src/main/java/com/ontology/platform/api/OntologyPlatformApplication.java"

[dev]
build = ["sh", "-c", "mvn clean compile -DskipTests -q"]
run = ["sh", "-c", "mvn spring-boot:run -pl ontology-api -am -DskipTests -Dspring-boot.run.jvmArguments=\"-Dserver.port=${DEPLOY_RUN_PORT:-8080}\" -Dspring-boot.run.profiles=dev"]

[deploy]
build = ["sh", "-c", "cd Ontology-platform && mvn clean package -DskipTests -q"]
run = ["sh", "-c", "cd Ontology-platform && export DEPLOY_RUN_PORT=${DEPLOY_RUN_PORT:-8080} && java -Dserver.port=${DEPLOY_RUN_PORT} -Dspring.profiles.active=dev -jar ontology-api/target/ontology-api-1.0.0-SNAPSHOT.jar"]
```

**功能**:
- 支持 `coze dev` 一键启动开发环境
- 支持 `coze build` + `coze start` 生产部署
- 自动绑定 `${DEPLOY_RUN_PORT}` 环境变量
- 默认使用 `dev` profile（PostgreSQL + Flyway + MyBatis-Plus；需先 `docker compose up postgres redis`）

---

### 3. 创建 `application-dev.yml` 开发配置

**文件**: `ontology-api/src/main/resources/application-dev.yml`

**核心配置**（2026-06-16 修订）:
| 配置项 | 值 | 说明 |
|--------|-----|------|
| 数据库 | PostgreSQL（继承 `application.yml`） | Flyway 脚本含 PG 专有语法，**不支持 H2** |
| 端口 | `${DEPLOY_RUN_PORT:-8080}` | 自动绑定环境变量 |
| JPA ddl-auto | `none` | 建表由 Flyway 负责，非 Hibernate |
| Flyway | `enabled: true` | 与 MyBatis-Plus 配套 |
| Redis | localhost:6379 | Phase 2 Job / 限流 / Webhook 依赖 |

**前置**:
```bash
cd docker && docker compose up -d postgres redis
```

---

### 4. 更新 README.md

**修改章节**:

| 章节 | 变更内容 |
|------|----------|
| 项目结构 | 添加 `.coze` 文件到目录结构 |
| 快速开始 | 重构为三种启动方式（Coze CLI / Docker Compose / 本地 Maven） |
| 开发环境配置 | 新增 H2 内存数据库说明和配置切换方法 |
| Coze 配置 | 新增章节展示 `.coze` 文件内容 |

**启动方式对比**:

| 方式 | 命令 | 依赖 | 适用场景 |
|------|------|------|----------|
| Coze CLI | `coze dev` | PG + Redis | 本地开发 |
| Docker Compose | `docker compose up -d` | Docker | 完整环境（含 MCP） |
| 本地 Maven | `mvn spring-boot:run -pl ontology-api -am` | PG + Redis | 自定义启动 |

---

## 三、验证

### 编译验证
```bash
cd Ontology-platform
mvn clean compile -DskipTests -q
```
**结果**: BUILD SUCCESS ✅

### 项目结构验证
```
ontology-platform/
├── ...
├── ontology-api/src/main/resources/
│   ├── application.yml          # 生产配置 (PostgreSQL)
│   └── application-dev.yml      # dev 覆盖（Flyway + 调试日志）✅
├── .coze                        # Coze CLI 配置 ✅
└── README.md                    # ✅ 更新
```

---

## 四、最终总结

### 今日完成

```
Java 运行时:   安装 OpenJDK 21 + Maven 3.8        ✅
开发配置:      修正 application-dev.yml (PG+Flyway)  ✅
.coze 配置:    入口类修正 + 迁入子仓库              ✅
README 更新:   去除 H2 误导，明确 PG/Redis 前置      ✅
─────────────────────────────────────────────────────────────
合计:          2 新增文件 | 1 修改文件
```

### 版本里程碑

```
v2.2 ✅  Phase 2c Metrics+ToolVer+mTLS
v2.3 ✅  +Coze CLI 支持 + dev profile 修正    ← 当前
v3.0     Docker 运行时验证 → 生产就绪
```

### 后续建议

1. **Docker Compose 集成** — 验证 `docker/` 目录配置完整性
2. **MCP Server 启动** — 确认 Node.js 22+ 环境可用性
3. **完整 E2E 测试** — 启动 PG + Redis + App + MCP 全链路验证

---

## 五、快速启动命令

### 开发环境
```bash
cd docker && docker compose up -d postgres redis
coze dev
# 或
mvn spring-boot:run -pl ontology-api -am -Dspring-boot.run.profiles=dev
```

### 生产环境
```bash
# 使用 Coze CLI
coze build
coze start

# 或使用 Docker Compose（需 PostgreSQL + Redis）
cd docker
docker compose up -d
```

### 访问端点
| 服务 | 地址 |
|------|------|
| API 基础路径 | `http://localhost:8080/api` |
| Swagger UI | `http://localhost:8080/api/swagger-ui.html` |
| 健康检查 | `http://localhost:8080/api/actuator/health` |

---

## 六、勘误（2026-06-16 下午）

| 问题 | 修正 |
|------|------|
| 入口类写为 `OntologyApiApplication` | 应为 `OntologyPlatformApplication`（`api` 包下） |
| `application-dev.yml` 用 JPA `create-drop` + 关 Flyway | 改为 `ddl-auto: none` + Flyway 开启，与 MyBatis-Plus 栈一致 |
| 宣称 H2 可无 Docker 启动 | Flyway 脚本依赖 PostgreSQL（uuid-ossp、JSONB 等），dev 需 PG；Phase 2 还需 Redis |
| `.coze` 仅在父仓库 | 已同步至 ontology-platform 子仓库根目录 |
| `application.yml` 重复 `server:` 键 | 合并 ssl 至首段 `server`，消除 SnakeYAML 启动失败 |
| dev 启动验证（2026-06-16） | YAML 通过后因 **localhost:5432 PG 未监听** 失败；本机无 Docker CLI，需先起 PG+Redis |
